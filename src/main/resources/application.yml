spring:
  application:
    name: springboot_Demo_01
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssm?characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456
  redis:
    host: localhost
    port: 6379
    database: 0
    jedis:
      pool:
        max-active: 8
        max-wait: 1ms
        max-idle: 8
        min-idle: 0

server:
  port: 8081
#mybatis:
#  mapper-locations: classpath:**/mapper/*.xml
#  configuration:
#    map-underscore-to-camel-case: true
